#include "Panels.hpp"

using namespace std;
using namespace sf;
using namespace klui;

/** Panel Class **/
/** Panel class that defines a panel areas within the 
 *
**/

void Panel::draw()
{
    //if(world_ != NULL)
    //{
    //    glPushMatrix();
    //    glMatrixMode(GL_PROJECTION);
    //    glLoadIdentity();
    //    gluPerspective(45,(float)getWidth()/getHeight(),0.01,1000000);
    //    glMatrixMode(GL_MODELVIEW);
    //    glLoadIdentity();
    //    glTranslated(0., 0., -camera_->getTz());
    //    glTranslated(camera_->getTx(),camera_->getTy(),0.);
    //    glRotated(camera_->getRotx(),1,0,0);
    //    glRotated(camera_->getRoty(),0,1,0);
    //    
    //    world_->draw();

    //    glPopMatrix();
    //}
    
    //glMatrixMode(GL_PROJECTION);
    //glPushMatrix();
    //glLoadIdentity();
    //glOrtho(0, getWidth(), getHeight(), 0, -1, 1);
    //glMatrixMode(GL_MODELVIEW);

    //for(std::vector<UIElement *>::const_iterator child = getChildren().begin() ;
    //    child < getChildren().end() ; child++)
    //{
    //    (*child)->draw();
    //}
    //glMatrixMode(GL_PROJECTION);
    //glPopMatrix();
    //glMatrixMode(GL_MODELVIEW); 
}
